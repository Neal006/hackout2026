---
name: Feature request
about: A change to the scheduler, API, apps or docs
labels: enhancement
---

**Problem this solves**
(who is hurt today and how — a driver, a site operator, the grid, a maintainer)

**Proposed change**

**Where in the code**
(e.g. `noonshift/scheduler.py solve_lp`, `noonshift/api.py resolve`, `wattwise/`)

**How we would know it works**
(a test, a `prove.py` number, a smoke step)

**Does it touch the frozen contract?** (`solve` / `impact` / `price` signatures, WS frames, REST models)
- [ ] no
- [ ] yes — additive only, described above