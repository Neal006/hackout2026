---
name: Bug report
about: Something behaves differently from what the docs say
labels: bug
---

**What happened**

**What you expected**

**How to reproduce**
```
# commands, or the demo button, or the API call
```

**Evidence**
- `python -m pytest` result:
- `python scripts/prove.py` output (if the scheduler is involved):
- Browser console / `api.log` lines (if a front-end is involved):

**Environment**
- OS / Python / Node:
- `SIM_SPEED`, `OCPP`, `DATABASE_URL` set?